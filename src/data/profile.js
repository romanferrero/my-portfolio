/**
 * Profile-level static data that doesn't fit elsewhere.
 *
 * PROFILE_PHOTO:
 *   Path to your portrait shown in the About section. Must live in
 *   /public so Vite serves it from the site root.
 *
 *   Set to "" (empty string) to hide the photo and fall back to the
 *   text-only About layout.
 *
 *   Recommended specs:
 *     - Square crop (1:1)
 *     - 800x800 px or larger
 *     - JPG or WebP (smaller files than PNG for photos)
 *     - Clean background, well lit, professional look
 *
 *   Example: place your photo at public/me.jpg, then set:
 *     export const PROFILE_PHOTO = "/me.jpg"
 */
export const PROFILE_PHOTO = "/me.jpg"
